server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Abhishek010397/Airflow-Docker-compose
          clone-on-start: true